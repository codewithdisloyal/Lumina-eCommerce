Place the image you attached (the rounded white card) in this folder with the filename `hero-shape.png`.

Path: assets/hero-shape.png

If you don't have the exact filename, either:
- Rename your image file to `hero-shape.png`, or
- Update the `src` attribute in `index.html` (<img id="hero-image" src="assets/hero-shape.png">) to match your filename.

Recommended size: ~900x900px (square) for good quality and to allow cropping via CSS `object-fit: cover`.
